[build-system]
requires = ["setuptools==84.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "csv-power-tool"
version = "3.2.0"
description = "Professional-grade CSV file combiner and processor with GUI and CLI modes."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "customtkinter==5.2.2",
    "tkinterdnd2==0.6.2",
    "darkdetect==0.8.0",
    "packaging==26.3",
    "chardet==7.5.1",
    "openpyxl==3.1.5",
    "et-xmlfile==2.0.0",
    "pyarrow==25.0.1",
    "polars==1.43.2",
    "polars-runtime-32==1.43.2",
    "rapidfuzz==3.14.3",
    "duckdb==1.5.5",
]

[project.optional-dependencies]
dev = [
    "pytest==9.1.1",
    "pyinstaller==6.22.0",
    "ruff==0.15.22",
]

[project.scripts]
csv-power-tool = "csv_power_tool.cli:main"

[tool.setuptools]
py-modules = ["CSV_Consolidator"]
packages = ["csv_power_tool"]
