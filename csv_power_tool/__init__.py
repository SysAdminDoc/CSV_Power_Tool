"""Testable adapters for the CSV Power Tool launcher.

The historical single-file launcher remains the compatibility entry point. The
modules in this package define dependency-injected seams for core processing,
CLI dispatch, GUI startup, and the loopback API without importing tkinter at
module import time.
"""

